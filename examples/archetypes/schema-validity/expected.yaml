# DRAFT — pre-schema
decision: APPROVE
firing:
  - rule_intent: schema-validity / catalog-v1
    obligation: schema-valid
    outcome: satisfied
    detail: "core-services.json validates; tags append only"
