# Change proposals

In-flight spec change proposals (proposal → specs delta → design → tasks). A change merges
into `openspec/specs/` when accepted and implemented. Empty until meta-plan Phase 3 starts.
